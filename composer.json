{
    "name": "mattyg/fb-privacy-auth",
    "description": "Implement authorisation checks based on the model Facebook uses for privacy controls for content (simple users and groups system).",
    "require": {
        "php": "~5.6.0|~7.0.0"
    },
    "require-dev": {
        "mattyg/array_column_maintain_keys": "~1.0"
    },
    "autoload": {
        "psr-4": {
            "MattyG\\FBPrivacyAuth\\": "src"
        }
    }
}
